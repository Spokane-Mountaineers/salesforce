<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Snapshot_Key__c</fullName>
    <description
  >YYYY-MM key for the snapshot month. Unique external id so the batch upserts exactly one row per month (Date fields cannot be external ids).</description>
    <externalId>true</externalId>
    <label>Snapshot Key</label>
    <length>7</length>
    <required>true</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>true</unique>
</CustomField>
